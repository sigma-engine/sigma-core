#ifndef SIGMA_PBR_DEFFERED_POST_PROCESS_EFFECT_GLSL
#define SIGMA_PBR_DEFFERED_POST_PROCESS_EFFECT_GLSL

// TODO this should be defined by scshader when .eff and the .frag they need are combined into one
#define SIGMA_PBR_DEFFERED_POST_PROCESS_EFFECT
#include <pbr/deffered/geometry_buffer.glsl>
#include <vertex.glsl>

#endif // SIGMA_PBR_DEFFERED_POST_PROCESS_EFFECT_GLSL
