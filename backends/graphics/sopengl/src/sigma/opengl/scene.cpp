#include <sigma/opengl/scene.hpp>

namespace sigma {
namespace opengl {
    std::size_t scene::create_point_light(graphics::point_light light_data, glm::vec3 location, float radius)
    {
        // TODO do this
        return 0;
    }

    void scene::update_point_light(std::size_t light_id, graphics::point_light data, glm::vec3 location, float radius)
    {
        // TODO do this
    }

    void scene::remove_point_light(std::size_t light_id)
    {
        // TODO do this
    }
}
}
