#ifndef SIGMA_ENGINE_OPENGL_POST_PROCESS_EFFECT_GLSL
#define SIGMA_ENGINE_OPENGL_POST_PROCESS_EFFECT_GLSL

// TODO this should be defined by scshader when .eff and the .frag they need are combined into one
#define SIGMA_ENGINE_POST_PROCESS_EFFECT
#include <geometry_buffer.glsl>
#include <vertex.glsl>

#endif // SIGMA_ENGINE_OPENGL_POST_PROCESS_EFFECT_GLSL
